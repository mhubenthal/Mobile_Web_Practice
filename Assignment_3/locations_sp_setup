-- Setup "locations" table in Postgres from "places" table.

SELECT city, state, county, type, preferred, worldregion, country, locationtext, location INTO TABLE locations FROM places;

ALTER TABLE locations ADD COLUMN location_id SERIAL;

UPDATE locations SET location_id = nextval(pg_get_serial_sequence('locations','location_id'));

ALTER TABLE locations ADD PRIMARY KEY (location_id);

